{
        "name": "@reev/root",
        "author": "tseijp",
        "version": "1.0.0",
        "license": "MIT",
        "private": true,
        "sideEffects": false,
        "scripts": {
                "build": "npm run tsup:core && npm run builds",
                "builds": "cd examples/docs && npm run build",
                "start": "cd examples/docs && npm run start",
                "dev": "cd examples/docs && npm run start",
                "serve": "cd examples/docs && npm run serve",
                "watch": "cd packages/core && npx jest --watch --runInBand",
                "cov": "cd packages/core && npx jest --coverage",
                "ls": "cd packages/core && npm publish --dry-run",
                "tsup:core": "cd packages/core && npm run tsup",
                "tsup:gesture": "cd packages/gesture && npm run tsup",
                "tsup:external": "cd packages/external && npm run tsup",
                "tsup": "npm run tsup:core && npm run tsup:gesture && npm run tsup:external",
                "vers": "npm run changeset version",
                "pub": "npm run tsup && npm run changeset publish"
        },
        "devDependencies": {
                "@changesets/cli": "*",
                "jest": "*",
                "tsup": "*",
                "ts-jest": "*",
                "typescript": "*"
        },
        "engines": {
                "node": ">=14"
        },
        "workspaces": [
                "examples/*",
                "packages/*"
        ]
}
