# How to use geolocation

## create your app

```ts
import { useGeolocation } from 'reev/external/geolocation/react'

export const App = () => {
        const { geolocation = {} } = useGeolocation()
        const { coords = {} } = geolocation
        return (
                <div>
                        <div>latitude: {coords.latitude}</div>
                        <div>longitude: {coords.longitude}</div>
                        <div>accuracy: {coords.accuracy}</div>
                </div>
        )
}
```

import { useGeolocation } from 'reev/src/external/geolocation/react'

export const App = () => {
        const { geolocation = {} } = useGeolocation()
        const { coords = {} } = geolocation
        return (
                <div>
                        <div>latitude: {coords.latitude}</div>
                        <div>longitude: {coords.longitude}</div>
                        <div>accuracy: {coords.accuracy}</div>
                </div>
        )
}

<App />
