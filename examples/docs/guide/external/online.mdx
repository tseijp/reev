# How to use online

## create your app

```ts
import { useOnline } from 'reev/react'
//                or from '@reev/external/react'

export const App = () => {
        const { online } = useOnline()
        return <span>{online ? '📶' : '❌'}</span>
}
```

import { useOnline } from 'reev/react'
//                or from '@reev/external/react'

export const App = () => {
        const { online } = useOnline()
        return <span>{online ? '📶' : '❌'}</span>
}

<App />
