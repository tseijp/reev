# How to use online

## create your app

```ts
import { useOnline } from 'reev/external/online/react'

export const App = () => {
        const { online } = useOnline()
        return <span>{online ? '📶' : '❌'}</span>
}
```

import { useOnline } from 'reev/src/external/online/react'

export const App = () => {
        const { online } = useOnline()
        return <span>{online ? '📶' : '❌'}</span>
}

<App />
