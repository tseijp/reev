# How to use clipboard

## create your app

```ts
import CodeBlock from '@theme/CodeBlock'
import { useClipboard } from 'rexr/react'

export const App = () => {
        const { clipboard } = useClipboard()
        return <CodeBlock>{clipboard || 'PLEASE COPY'}</CodeBlock>
}
```

import CodeBlock from '@theme/CodeBlock'
import { useClipboard } from 'rexr/src/react'

export const App = () => {
        const { clipboard } = useClipboard()
        return <CodeBlock>{clipboard || 'PLEASE COPY'}</CodeBlock>
}

clipboard result

<App />
